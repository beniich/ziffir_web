import { app } from '../server.js';

// Ce fichier sert de point d'entrée pour les fonctions Serverless de Vercel.
// Vercel injecte `req` et `res` directement dans l'application Express exportée.
export default app;
